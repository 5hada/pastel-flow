import { ToolsPanel } from './ToolsPanel'
import type { ToolsProps } from '../../shared/layouts/Workspace'

export function ToolsWorkspace(workspaceProps: ToolsProps) {
  return <ToolsPanel {...workspaceProps} />
}
